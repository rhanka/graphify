# Python Workspace

This directory contains the Python implementation of graphify.

- `graphify/` contains the Python runtime modules and packaged skill templates.
- `tests/` contains the Python test suite and Python-side fixtures.

For now, Python packaging is still configured from the repo-root `pyproject.toml`,
which points setuptools at this workspace.
